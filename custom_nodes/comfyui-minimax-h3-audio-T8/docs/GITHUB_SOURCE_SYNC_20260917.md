# GitHub 源码同步与发布边界（2026-09-17）

同日后续 Semantic Bridge 已发布 v1.84.0；H16 已完成部分另外作
[源码更新](H16_SOURCE_UPDATE_20260917.md)。本文下方 v1.83.0／未发布范围与测试数
保留为当时快照，不覆盖这些后续交付；未完成 H16-3／Meridian 仍不发布。

这是 v1.83.0 之后的源码更新，不建立新版本号、tag 或 Registry 发行包。
下方历史开发记录中的“本地／未发布”是当时的状态；当前线上内容以真实提交为准。

## 本次合并范围

- 以已发布的 `4a9269ffe68d868192bb78d96da615c0db065ee8` 为基线，合入已完成的
  用户补丁警告放行策略、原样 Sol 节点注册、非 PDD 时间分块及倍率／INT 尺寸输出、
  SelfLift 五项真实接口修复。未知组合不等于禁止使用，也不等于通过效果认证。
- 保留线上已有 Topaz 自动 H264 交付、音频保留、手工参数说明，VRetouchEr 研究包排除规则，
  以及 Registry 激活检查，不用旧正式副本覆盖较新的已发布修复。
- 保留 Qwen 前缀缓存每次 tokenize／encode 时重新检查实际权重／LoRA 身份的修复及回归测试。
  未识别 CLIP 只绕过该优化，不替换用户 CLIP，也不借旧缓存冒充新输入。
- FastH3 V2 遇到后置用户 attention／DiT owner 时保留原 callable。
  内存桥接不能安全重建该组合时跳过 VSA 头分组重绑定并警告，不清空用户替换。
  这类组合使用不可跨运行复用的身份；不透明补丁不能被归一化成原纯净 V2 的缓存身份。
- 保留已发布七个独立 EAV 节点与内循环的新建节点默认应用区间 15%–90%；正式副本恢复线上默认，
  不迁移旧工作流显式写入的0/1参数，也不改变SelfLift内部显式时间表。
  已验收长视频示例、AV 时钟、音频、sigma、上下文、3D 网络与接缝数值配方不改。

非 PDD Chunked 示例仍是 EXP：一采 4 步，每个二采时间窗 4 步；两个时间窗合计 12 次前向，
不是整条视频总共 8 次。原机械验证不等于新增完整模型 GPU 画质验收。

## 明确不发布

- 另一任务尚未交付的 Meridian／Semantic Bridge／H16 准备代码、转换产物和未完成测试。
  其准备目录保留在本地，不接入正式注册、元数据或推荐示例。
- 失败实验、模型、人物图、视频、测试产物和本地交接文件。
- `ROADMAP.md`（包括大小写变体）及其历史；`SKILL.md` 也不随本次源码提交上传。

用户随后明确要求私人改装图一起发布，因此另附
`2026-09-17_H3_Chunked_4plus4_接线修正版（低显存双分块双采样）.json`。
它保留自定义参数与 KJ／Sol／LoRA 链，作为用户风险自担的 EXP 示例，不借用纯净图的画质资格。
依赖与实际尺寸／时长见[时间分块说明](CHUNKED_STANDARD_4PLUS4_EXP.md)。

## 为什么正式目录会与线上不同

此前有更新从独立本地发布树推送，但正式目录只回写了部分版本说明和配置；
例如正式 Topaz 五个运行模块仍对应 v1.79.6，而线上已含后续自动交付和参数说明。
这属于正式部署回写不完整，不是 GitHub 自行生成了较新代码。
版本号、Git HEAD 和磁盘上的实际运行模块必须分别核对。

## 后续防回归门禁

1. 固定真实线上 SHA，按已完成源文件白名单合并；先查其他任务的未完成准备件。
   不整棵复制研究目录，不使用 `git add .` 包含脏目录，不强推覆盖线上历史。
2. 核对实际注册顺序、Core 输入／输出与已保存图；回归缓存身份、原 callable 保留／委托、
   真实错误传出，以及[五项长视频接缝门禁](DUAL_MODEL_SEAM_FIX_20260913.md)。
3. 线上已有修复的代码和测试一起保留。测试只能更新真实的策略／追加注册断言，
   不删数值、损坏 receipt、坏哈希或真实 kernel 异常用例来制造通过。
4. 正式目录同步前备份目标文件并检查同步期间没有其他任务修改。
   逐文件比对实际内容；故意保留的历史默认差异要明确记录，不能宣称整棵目录完全一致。
   不 reset 用户 index，不删除根目录旧兼容入口，不重启用户正在使用的 ComfyUI。
5. 推送前、推送后都检查候选文件清单和全部祖先历史不含 ROADMAP；
   核对远程提交 SHA。源码更新不冒称 Registry 已发布或已激活。

本次版本保持1.83.0。因现有Actions会在pyproject变化时尝试Registry发布，
源码提交使用CI跳过标记避免无授权重复上传同版本；本地仍执行版本一致性、文件白名单、
完整祖先ROADMAP检查及受影响CPU/Core检查。不能把跳过的Actions写成线上CI通过。

CPU／实际 Core 校验、GPU 内核实测、完整成片人审是三种证据，分别记录。
以后新增发行包必须重新验证实际新包，不能沿用历史 339 节点包或旧样片的冻结证据。

## 本次源码交付检查

最终受影响CPU批次覆盖92个测试模块，1821项通过、0失败、2个子进程worker入口条件跳过，
源冻结1678文件无变化、CUDA未初始化。实际子进程恢复／逐位对照已执行；
安装版及两个本地历史版本KJ的108项外扩组合检查均执行，没有借可选依赖缺失跳过。
五项接缝门禁、FastH3 V2后置owner保留／不可移植身份、Qwen实际权重身份、
已发布Topaz运行修复、EAV默认区间和SelfLift原五项接口错误均在本次范围内。

一项外部集成检查被明确排除：用户工作流目录中的旧README索引镜像与已发布索引不一致。
失败证据保留，不删除原测试、伪造镜像一致或覆盖用户目录来制造通过；
这不是完整仓库全量通过声明。

实际Core注册340个唯一ID；原标准分块图22节点／41连线／78参数、三份未改SelfLift图，
以及私人改装图30执行节点／49连线均通过API校验。改装图仅校验实际已安装外部原类schema，
没有执行其GPU kernel。原Sol源码与原标准图SHA不变；私人图只改备注、语义相同的Ref2VA值
和当前SaveVideo控件序列，数值参数及49条连线不变。
当前检查不代表新增完整模型GPU样片、第三方全组合画质、人审或新Registry发行包验收。
