# 双模型潜空间放大长视频（开发中）

> 已随v1.79.0发布为可选EXP功能。以下保留开发验证范围，不代表所有材料与组合都通过。新模板在项目工作流目录，旧流程不变；见[更新说明](RELEASE_1.79.0.md)。

**2026-09-13最新覆盖：** 正式版已验收的`accepted_picture_low_context_v1`与8秒示例已移入研究树。下一段LOW参考取前段成片末39帧，经既有resize/VAE；HIGH、音频、4+4和3D放大不改，旧JSON默认不变。失败latent桥移到测试夹具。详见[精确实现与防回归](DUAL_MODEL_SEAM_FIX_20260913.md)。移植CPU验证不等于新增GPU质量验证，也未解决Dance/Depth的人审拒绝。

**此前实验记录（不是当前操作计划）：** 用户已接受8秒恢复、KJ分块、I2VA和独立底模指定样片的评分项。
Sol1.3曾闪烁并出现重复人物；新增H3非视频精确Q/KV保护后的显式0.5短片，
画面、音乐人声和口型已获用户认可。原24秒及颜色修复版接缝仍失败，不改写旧记录。
2026-09-12新增二采重叠锁定：两段8秒KJ＋Relay样片的画面、音乐人声、口型、接缝
四项已获“正常／可接受”反馈，用户说“这次接缝似乎还行”。仅限该样片，不代表所有素材。
随后另一条0.4MP两段8秒片在人审中发现第123→124帧明显结构跳切：钢琴家和钢琴在
首个自由生成帧直接消失。联合VAE解码仍保留该跳切，证实根因不是分段VAE拼接；旧的
“前缀逐位一致”机械检查也不足以代表接缝通过。当前新增二采后潜空间桥，保存latent
latent复测曾把边界/邻帧中位数比从约6.0降到1.89，独立编码成片复核为1.84；但用户
完整看片后判定后半段持续虚影、乱晃并结构崩坏，因此该数字是错误的质量代理，方案人审失败。
更正：三阶段解码不能证明“一采未锁是根因”，`low_x0`是部分轨迹的预测，不是最终成片。
用户已指定原8秒MP4作为“只有一处跳切、后面没有崩坏”的基准。失败桥已从出片路径撤下，
诊断复现仅保留在测试夹具；原AAC保持不变。随后accepted-picture单变量方案已获指定8秒样片人审接受，
并非仅撤回错误桥接就宣称修复。22→39上下文的旧对照未解决问题，不是当前建议；不追加重跑已接受样片。
旧六次测速使用失败的1.3配置，不能拿来承诺新0.5候选的速度。

双模型循环现默认开启可关闭的`color_match`。它以校验过哈希的上一段已接受视频尾5帧
为参照，沿用既有Color Match算法，对续段开头24帧做有上限的颜色统一。没有单独的
可变状态文件；恢复时重新读取被接受的前段，不会串用重试候选。音频、潜空间与帧数不变。
现有24秒样片经实际新函数CPU重放，5处平均RGB跳变降低，原音频PCM逐位一致；
这是颜色修正，不代表人物/背景结构变化或所有接缝都已验收。
旧JSON不改端口次序；新增开关追加到末尾，两个交付模板都明确开启。
这一轮改动已有针对性回归，旧4513完整回归仅是此前源码证据，不冒称新源码全量重跑。

两个新交付模板默认8秒，显式选`video_context_mode=high_native_mask_exp`：二采前，
将上一段最终高分辨率视频尾部放入已知区域，并在采样时锁定该区域。已知音频遮罩不变，
新声音继续完成采样。22帧上下文对应7个视频潜空间时间单元，实测前缀最大差2.38e-7。
二采后的5-token/约17帧潜空间桥已被人审判定失败，已从正常出片路径撤下。
只有诊断复现函数保留。桥接对照不能证明一采原本有错，也不能据此规定两阶段都必须硬锁。
下一步按原片基准做控制变量验证，并检查完整续段，不以边界单帧差值替代人审。
接缝位置约5.17秒，不要求两段各恰好4秒。
旧工作流缺省仍为`reference_only`；节点默认值不强改。切换该模式时使用新的chain_id，
不能复用另一模式的阶段缓存。8秒新模板不缩减长视频能力，时长仍可自行调整。

这是独立的新节点和两份候选工作流。旧的内循环、Stock20 和手动二采工作流不变。
之前的短片和长链虽通过文件检查，但用户指出多组人声崩坏。现已修复4+4提前锁住未完成音频，以及续片模板覆盖本段音频的问题。用户确认新版3秒KJ样片声音正常；修复后的三后端短片、8秒中断恢复、24秒链、游戏及两套完整底模均已完成指定用例的文件及实际调用检查，不能据此替代完整画质、声音和口型人审。
不要把本文当作已发布的兼容承诺或性能结果。

当前补验更新：修复后的PyTorch、Sol、KJ省显存head4/FFN2+Relay和首帧I2VA
短片已分别完成机械审计，不能直接用不同内存设置比较速度。24秒Relay修复链已完成6段、576帧；逐段音频续采、已知音频区域不变、文件解码和115个实际计时区间通过检查，画面、声音和接缝仍待集中人审。
两套完整底模先前在加载时触发系统内存保护；可用内存回升后，修复后的FL2VA一采／Ref2VA二采
补验已通过，详见[内存记录](DUAL_DISTINCT_BASE_RAM_20260911.md)。不把旧版冻结未完成音频的
样片当成本次通过证据；下面单独注明的Stock20/EAV历史记录也不代表新4＋4人审通过。

最新完整CPU回归为4513通过、136跳过（v5）；1458个受检源文件未在运行中变化，未初始化CUDA。此前v4的FFmpeg素材准备失败单独保留，不篡改为通过。文末六次测速是当前音频修复后的同条件结果，已核对源码、输入、实际后端、缓存、媒体和内存设置；不是画质人审或已发布承诺。

## 接线与参数

工作流文件夹内附有[KJ/Sol接线示例](../examples/workflows/04-long-video/DUAL_MODEL_ACCELERATOR_CONNECTIONS.md)，包括具体节点类、先后顺序和已验证配置。模板不强制安装这些外部节点。

### 2026-09-12 PyTorch 2.9+ 与外部 LoRA 修正

双模型身份记录不再同时读取后端专用 `cuda.matmul.fp32_precision` 与旧版
`get_float32_matmul_precision/allow_tf32`。PyTorch 2.9+ 存在后端专用接口时只记录该接口；
旧版 PyTorch 没有该属性时才读取旧接口，避免用户环境已设置新版精度后在采样前被“mix of
the legacy and new APIs”错误中止。相同修正也用于原生扩画模型身份记录。

`MiniMaxH3LoRACompatibilityLoaderT8Advanced` 新增显式 `disabled` 选项。每个双模型阶段可在
自己的 Turbo LoRA 后再接一个该加载器作为可选外部 H3 LoRA；默认 `disabled` 直通且不打开文件。
外部文件仍必须放在 ComfyUI 的 `models/loras` 搜索路径，并由结构映射确认实际 target；没有兼容
target 时报告 `no_compatible_patches`。断点身份会哈希当前 Core 的 `LoRAAdapter` 权重、顺序和强度，
两路外部 LoRA 不会因为文件名相同而被当作同一执行配置。

新增 PyTorch、KJ Sage、官方 Sol 三份互斥后端工作流。每份两路均独立复制 LoRA 与 Attention
节点，不再使用“一条补丁链最后分叉”的接法，也不把 Sage、PyTorch selector 和 Sol 串起来。
Sol 工作流只使用已审计的 `SolAttentionPatch`；`SolAttnMiniMax / SolAttn_triton` 仍未认证，不能
通过放宽身份保护强行接入。Relay 的偏置及不等长 Query 仍走明确的精确回退。

两路都是“底模 → 该路自己的 LoRA → model_pass1 / model_pass2”。
可以共用同一底模、分别选择 LoRA，也可以另接兼容的原生 H3 底模。
本模板不使用 VDN；VDN 的8+4不是本节点的4+4。

- `low_width / low_height`：一采的画幅。模板512×256。
- `width / height`：最终画幅。模板1024×512。
- `coarse_steps=4`：一采使用完整8步表的前4步，但交给放大器的是预测的干净潜空间 x0，不是带噪声的中间结果。
- `refine_steps=4`：二采按独立模型的双时钟和既有放大细化时间表执行4步，结束于sigma0；不是简单复制一采时间表的后4个数。
- `second_audio_source=auto`：4+4由二采继续完成音频；只有完整Stock20一采才默认锁定声音。旧候选4+4的first_pass/0设置自动迁移并记录，避免提前冻结半成品音频。两种分辨率的续段均使用最终音频。强度参数在auto/native legacy模式下不覆盖原生遮罩；口型与内容仍需看真实成片。
- `video_context_mode=high_native_mask_exp`：在二采过程中锁定已知视频前缀，不再做采样后视频桥接。22/39帧上下文均支持。“一采未锁导致崩坏”没有因果证据，已撤回。改变上下文或实验逻辑时换新`chain_id`，保留原片；完整续段仍须人审。

续片时，二采只在原生遮罩为0的位置使用高分辨率条件中的已知声音；可采样位置保留本段一采结果，再由二采完成，不会因一小段上下文而替换整条音频。低、高两路下一段上下文都保存最终二采声音。该修正已覆盖CPU测试与真实8秒两段中断恢复，画音人审及更长链仍待。
- `coarse_steps=20`：为原始Stock20及EAV保留；4+4时EAV必须关闭。
- `chain_id`：断点任务名字。修改提示词、模型、LoRA、时长或尺寸时换一个名字；不手动混用不同任务的缓存。

两份JSON在 `examples/workflows/04-long-video/`，直接导入。节点自己保存并预览视频，
不需要从前端“导出API”才能使用。基础图用global_prompt；Relay图每行一个局部事件，
并按时间范围投影到各段。改变总时长，也要同步Plan的length。

全局提示词会用于每一段，不是一次性执行的完整剧本。模板将稳定场景/音乐留在全局，
单次对白只写进Relay第一项局部事件；基础图默认纯音乐。
修复后的24秒六段链已完成576帧及音画完整解码检查：已知音频上下文保持不变，
本段新声音继续二采完成。23秒抽帧仍可见不需要的字幕状文字，尽管提示词明确禁止文字。
这是真实存在的质量问题，不能裁掉后当作通过，也尚不能确定是模型能力还是其他原因。
仍需完整听看事件跟随、对白出现次数、音乐、口型和接缝。

## 模型

复用现有模型，不需要新训练或转换。候选模板选择：

- `models/diffusion_models/minimax_h3_fl2va_int8_convrot.safetensors`
- 两路默认都用 `models/loras/minimax_h3_turbo_v4_step600_ema_comfyui_B.safetensors`，可分别更换。
- `models/latent_upscale_models/minimax_h3_latent_upscaler_3d_fp16.safetensors`
- 已有H3 Qwen文本编码器、视频VAE和音频VAE。

## 当前边界

修复后的KJ普通Sage、普通Sol和原生PyTorch均完成两次3秒4+4实际执行和独立检查，
两采分别记录目标后端200次调用，无静默回退。新版KJ省显存Sage＋Relay也完成
head_chunks=4、FFN chunks=2/threshold=4096组合检查；不是所有分块值或人审通过。
浮点Relay偏置不会为了加速被删除。普通Sol不能执行带该偏置或不等长query的调用，
需要明确回退；另一种SolAttn_triton的共享hooks/Morton方案尚未认证。
两份模板不强制预接外部加速器，连接方式见前面的KJ/Sol示例。

追加的KJ组合测试已完成：两路都接省显存Sage、LowVRAM head_chunks=4、
FFN chunks=2/threshold=4096，再接Relay。3秒4+4实测及独立检查通过，
两路报告均保留上述配置，Sage无回退；修复后版本保留音频续采输入，再完成二采，
不把“最终音频等于未完成的一采”当成通过条件。
这不是所有分块值、EAV叠加或显存/速度收益验证；画质需集中审片。

音频修复前另有EAV组合记录：一采为不挂Turbo的原始20步，二采挂EMA B做4步，
叠加上述head4/FFN2、KJ省显存Sage和Relay。实际20+4次前向、1000次EAV
观测及音频潜空间逐位保留通过检查。这不等于允许4步Turbo一采开启EAV。
两份工作流也已在隔离前端改二采LoRA强度、保存、重载、导出原生API并独立
核对参数和连线；Relay时间段改动一并通过。正式模板默认值未因此改动。

首帧参考I2VA也完成一条3秒1024×512的4+4实测：两阶段分别建立I2VA条件，
原图文件身份前后相同，Sage各200次无回退，修复后音频继续完成二采，成片完整解码通过。
二采条件报告在交付层effects sidecar中，已按任务、候选及阶段记录校验；
见`artifacts/dual-audio-repaired-i2va-headroom2-gpu-v1/independent-audit-v1.json`。
这不是参考图保真/口型人审或长片图生视频验收。

一采、放大、二采分别保存检查点，续段保留低/高两套尾部。恢复会核对实际模型、
LoRA强度/内容、分词器、输入媒体和本地运行代码身份，不只检查文件名。
这会增加开始时的CPU读取/哈希开销，报告单独记录；不能当作采样耗时。

16GB显卡不代表系统内存一定够。首条实测曾在一采初始化时触发系统RAM保护；
阶段间释放编码器/模型驻留后，基础与Relay短片均完成，原保护线没有降低。
这不保证所有尺寸、参考数量和其他软件占用下都不会超内存。
不同底模的修复后补验现已完成：一采FL2VA、二采Ref2VA，各自挂EMA B，实际内容身份不同。
新片完成4＋4步、Sage200＋200调用及音视频解码，音频由二采继续完成，不用旧片顶替。
这次启动可用RAM约101.65GiB、最低约10.79GiB；此前90.58GiB启动的一次在加载时触发RAM保护。
默认主机锁页缓存曾加重内存压力；候选实测使用隔离Core的`--disable-pinned-memory`。
该参数减少主机锁页缓存，不是Sage开关，也没有写进用户启动文件。
不能把共用底模的内存需求套到两套独立大底模上；可能牺牲搬运性能。
完整范围完成后，再集中交付画面、声音、接缝对照和实际后端/耗时报告。

## 同条件后端实测（单机少量观测）

以下表格来自音频修复后的六次同条件运行，旧锁音频样本及被其他音乐任务干扰的一组已排除。
三后端统一禁用锁页、reserve-vram=5、vram-headroom=2；未修改用户启动配置。
当前增加模型本地计时：初始采样准备、前向、两采、放大、AV解码、trim、候选保存和最终封装。
这些是主机调用墙钟时间；动态搬运可能包含在前向中，各层区间也会嵌套，不能相加成纯GPU耗时。
复用缓存时，旧阶段报告只是来源记录，不计作本次新采样。

RTX4060Ti16GB；固定Core488e8f8、Torch2.10.0+cu130；同一FL2VA底模、两路EMA B、
seed、提示词、512×256→1024×512、3秒4+4。每个后端新进程首次运行一次，随后在同一
进程再完整运行一次。关闭节点缓存，并更换链名防止复用已生成阶段；正常阶段卸载仍保留。

| 后端／运行 | 全图秒 | 一采秒 | 放大秒 | 二采秒 |
| --- | ---: | ---: | ---: | ---: |
| PyTorch首次 | 269.11 | 32.68 | 1.84 | 105.71 |
| PyTorch复跑 | 267.77 | 32.27 | 1.06 | 105.33 |
| KJ首次 | 233.87 | 30.87 | 1.85 | 74.89 |
| KJ复跑 | 236.49 | 30.97 | 1.09 | 75.29 |
| Sol首次 | 239.83 | 32.00 | 2.27 | 69.48 |
| Sol复跑 | 258.32 | 30.62 | 1.11 | 69.08 |

这几次KJ、Sol均比PyTorch少用总时间；本组KJ全图时间较短，但不能由两次观测确定稳定排名。不是多次中位数，
没有清除系统磁盘缓存，也不是模型永久驻留显存的热跑。采样计时包含内部模型加载／搬运；
模型身份检查、条件编码、VAE和封装包含在全图时间内，但这里没有分别计时，不能用差值
精确归因某一阶段。所有任务严格串行，不能把这些数值外推到24秒Relay或别的机器。

六条各实际执行4+4次前向、目标后端200+200次调用，无回退；保留一采音频作为续采输入，
最终音频由二采联合完成，并非逐位冻结未完成的一采。
每种后端的首次和复跑成片SHA相同，但不同后端不要求画面逐像素相同，质量仍需人审。
显存和系统RAM只做整机周期抽样，不是精确单进程或每阶段峰值，不用来宣称某后端更省显存。
证据：`artifacts/dual-repaired-backends-summary-v1.json`及`db-pt-v3`、`db-kj-v1`、`db-sol-v1`逐链审核。

English: this is a GitHub-scoped EXP feature with independent stage models/LoRAs, learned2x latent
upscaling and separate low/high video contexts with completed output audio. Partial4+4 continues audio in pass2; completeStock20 may retain first-pass audio. Existing workflows
remain unchanged. KJ and Sol figures above are one cold-process and one same-process observation
each, not medians or universal speed guarantees. Sol's biased/unequal-query cases are not certified
as sparse acceleration. Named machine checks do not replace collective visual/audio review.
