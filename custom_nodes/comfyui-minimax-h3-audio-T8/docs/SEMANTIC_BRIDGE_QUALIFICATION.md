# Semantic Bridge: qualification ledger

## v1.84.0 accepted repair (latest status)

On2026-09-17 the user accepted B on the8810 repair review page and authorized
formal workflow publication and GitHub push: “可以了没问题了”. This binds media
`6da418003510e040d2b3ccc7d9961dcb8d6b652395c0b50425f326159348ce9c`,
review_id `6ace2aecb00e6fc1a01705304547728410b626e1869a13ac82a739349b95e895`,
LOW640×320/HIGH896×448, independent4+learned3D+4, both Bridges0.10, two segments8s.
The old failed media and both unsuccessful bypass controls remain rejected.
The specified replacement is accepted; unassessed categories of other groups do
not become passes. The original formal repair scope444 tests/Core5 graphs passed.
Fresh release-source CPU/package checks are separate local receipts, not inferred
from the stale development tree or previous package-final-v2. H16/Meridian remain
paused; historical continuation instructions at the bottom are not current authorization.
The340-node baseline, Sol bytes, old workflows and runtime numerical route are retained.
See [release notes](RELEASE_1.84.0.md). This section supersedes historical pending/
not-authorized statements below for this release only; it does not rewrite failed evidence.

## 2026-09-17 Semantic Bridge-only completion rebase

New target: preserve the current formal340-node prefix including Sol and append only2 Bridge nodes (342 total). H16/Meridian remain paused and are not copied. Current-source CPU/Core/package/deployment results live in `artifacts/semantic-bridge-completion-20260917/verification.json`; do not infer an absent terminal is a pass. Upstream wrappers are published at https://huggingface.co/t8star/Semantic-Bridge-Comfy with full tensor parity and remotely verified bytes. Existing media below retain their original runtime identities and are not relabeled as newer-source GPU qualification. The collective human review has returned: eight groups have no reported failure; the right dual-loop picture was rejected for floating white fragments. Its music/speech, lip motion and seam were rated acceptable; unassessed categories are not passes. The replacement spatial candidate still requires its own complete picture/audio review.

## Rejected dual-loop picture: controlled follow-up

The rejected8s file is the `9ac4ecc4…` media below. The fragments exist at frame0,
before the124-frame seam. Two fresh formal-source controls preserved the same prompt,
seed, independent MODEL/LoRA branches, LOW4 → learned3D → HIGH4, output896×448,
audio and seam settings. HIGH-Bridge-only bypass produced SHA256
`8f79a258ab77d4959df4fd11fc63cd2724dc85037bfaef964147491985373cde`;
both-Bridge bypass produced
`7f81dfc05a81fabc49b721d6e37b410b6896f7d283f0b25c026b23c9ce81d874`.
Both full192-frame8s media strictly decode and still show floating light fragments.
Actual stage receipts record four completed forwards at each of the four stages;
the HIGH-only bypass retained the exact first LOW tensor. These are unsuccessful
diagnostic controls, not release examples or human-accepted fixes. They do not
establish an adapter formula bug, an upscaler bug, or a universal dual-sampling defect.

The spatial repair candidate changes only LOW448×224 to640×320 plus a fresh
chain name, retaining HIGH896×448 and both active Bridge configurations. Its original
prompt/seed, four-plus-four schedule, learned upscaler weights, audio policy and seam
strategy remain fixed. Complete generation, strict AV decode, content-bound stage
receipts and final-cache reuse must pass before machine qualification; human picture,
speech and seam acceptance are separate. Evidence is under the local
`artifacts/semantic-bridge-completion-20260917/human-feedback-20260917`.

That spatial candidate has now completed from the formal core source: SHA256
`6da418003510e040d2b3ccc7d9961dcb8d6b652395c0b50425f326159348ce9c`.
It retains four distinct encoding receipts from the two active adapter weights,
four completed forwards per stage,192 video frames at24fps and8.032s compressed
audio. Strict AV decode and verified-existing-final cache return passed; latent and
media files remained unchanged on resume. A24-frame completed-media inspection did
not show the original floating fragments; this is not a substitute for full human
playback or proof of which underlying module caused them. The final formal24-module
scope passed444 tests, with2036 frozen sources unchanged and no CUDA initialization;
all five actual-Core graphs validate. Other four example API recipes remain unchanged.
The updated spatial example and clearer Bridge-bypass notes are in the formal core,
not just a development tree. Human review of the new candidate remains pending;
GitHub push is paused and the older package needs rebuilding before a future release.

The current rebase and published T8 Compat weights have also completed one fresh
dual-MODEL4+4, active-Relay, two-segment8s probe. Four independent Bridge encoding
receipts and strict video/audio decode passed; completed-final cache reuse retained
latent/media bytes. Media SHA256
`9ac4ecc404560f05c51895f58a46d95601241437843765c8796c4d9027cce4a7`
matches the historical fixed dual recipe exactly. Only that bound review clip is
linked to the new-source receipt; other historical clips are not relabeled.
The optional whole-repository diagnostic collected6328 cases and was stopped after
3066 teardown phases, not passed. Thirty observed failures were compared against
the unchanged formal baseline:23 reproduced there and7 append-only registry/schema
assertions needed updating. Final scoped Bridge/affected checks are recorded separately;
do not describe this as whole-repository green. Failed and interrupted receipts are retained.

Final frozen scoped CPU regression passed443 cases with no failed/skipped/deselected
tests and no source changes. Actual Core validates all five rebuilt graphs and
preserves the340-node prefix. Formal disk sync and fresh formal-module rechecks
must be read from the completion receipt, not inferred from candidate tests.
This is local development evidence, not a release, human acceptance, or a universal
quality/performance claim. Historical artifact paths below are relative to their
original isolated development checkout.

## Completed machine checks

| Requirement | Authoritative evidence | Limits |
| --- | --- | --- |
| Six-tensor source and lossless wrapping | artifacts/semantic-bridge/p0-assets.json | New wrapper SHA differs; tensor content/dtype retained |
| Native / original / BUNNY, two seeds | p1-*seed91701 and91702; native91701 independent audit | Original native91701 controller JSON report failed; not relabeled a successful controller |
| INT8 text encoder three-way | p1b-int8-*-seed91701/terminal.json | Fixed encoder and recipe, no generic distribution equivalence |
| Actual first-frame Mandarin I2VA | p2b-i2va-mandarin-*-seed91703/terminal.json | User portrait ratio2:3, generated voice remains human-pending |
| Korean image-reference Ref2VA | p2c-ref2va-korean-*-seed91704/terminal.json | Does not establish reference-audio or Hybrid behavior |
| External/internal BUNNY equivalence | internal-external-parity.md | Bound decoded RGB and PCM only |
| Active Prompt Relay | p2-H3_SemanticBridge_Relay_EXP-v3/terminal.json | Correct base→Relay→LoRA order; failures retained |
| Single/dual loops, two segments8s | p2-H3_SemanticBridge_*Loop_8s_EXP-v4 and DualIndependent v4 | Completed-final cache reuse is not actual interruption recovery |
| Small CUDA mathematical contracts | cuda-math-v1 | Eighteen bounded model/mode/dtype cases, not video quality |
| Native frontend save and export | ui-native-v1/native-audit-v3.json and Reload API files | Five graph/API round trips; no generation or frontend injection |
| FastH3 V2 trained VSA with T8 memory and Bridge | p3-fastv2-vsa-h4c2-original-v1/terminal.json and generation/history.json output20 | 400 actual VSA dispatches over eight sigmas; no Dense dispatch; human-pending |

Paths in this table without a prefix are under `artifacts/semantic-bridge/`.
The exact saved graph/API identities, model hashes and server lifecycle evidence
are in their individual artifact receipts; similarly named old samples are not substitutes.

## Actual interruption: single and dual independently completed

`p2d-interrupt-DualIndependent-v1` retained a real accepted first segment, then the
owned service received POST /interrupt and Core reported execution_interrupted.
First media/context/stage identities and Bridge receipts were saved. The controller
stopped the first service, started a new process, but reused one telemetry guard
across a deliberate unmonitored service gap. Its first new sample was5.79s later,
exceeding the live3s gap policy. The resume was stopped before generation.

The phase-finalizer also attempted an exclusive write to the same progress.json a
second time, masking the telemetry error with FileExistsError. Both failures are
controller lifecycle bugs, not evidence of successful or failed second-segment
model inference. Both owned services are confirmed cleaned. Preserve the original
failed terminal and first-segment files. Recovery must verify all first-segment
hashes/mtimes and the exact interrupted contract, use a fresh service, and complete
the second segment without regenerating the accepted first segment.

That recovery subsequently completed in a new owned process15132 (original11532).
`p2d-recovery-DualIndependent-v1/independent-post-exit-audit.json` verifies the
actual interrupt history, all13 retained first-segment file hashes and mtimes,
the same accepted entry/contract, four Bridge receipts (two per segment),460
unchanged sources and all complete model hashes. The192-frame/8s H264+audio file
strictly decodes and is byte-identical to the independently completed dual v4
baseline: SHA256 `9ac4ecc404560f05c51895f58a46d95601241437843765c8796c4d9027cce4a7`.

The recovery controller itself remains failed: its first post-generation ffmpeg
check exited unsuccessfully with empty stderr, without preserving the exit code.
Both default-thread and bounded-thread independent full video/audio rechecks
passed on the unchanged file after owned server exit. The initial decoder failure
cause is unknown; do not relabel the failed terminal or claim its cause was proved.
Both generation service cleanup and the independent audit have terminal receipts.

The repaired two-phase controller then completed SingleLoop-v2: owned processes
3624 and23748, actual native interrupt history, unchanged accepted first files
including modification times, two independently encoded Bridge receipts, and no
cleanup errors. Its8s192-frame media SHA256
`795f104cbeb29535b5f1437f688b1cc133d365b717a92d30cf22733cccf5187c`
matches the uninterrupted SingleLoop-v4 output exactly. The controller terminal
is `native_interruption_new_process_resume_pass_human_pending`. Source/model
identities were checked after both phases. Both services exited; no additional
long clip was generated. This proves this fixed recipe's restart result, not
arbitrary inputs or human seam/audio acceptance.

## Hybrid qualification boundary

`p4-hybrid-voice-native-v1` was interrupted deliberately: the pruned Hybrid base
combined with full-model Turbo v4 emitted repeated Core AdaLN patch shape errors
before any Bridge was enabled. This invalid baseline is retained, not accepted.
The v2 comparison follows the existing Hybrid Stock20 workflow without Turbo;
native/original/BUNNY arms have identical reference, prompt, seed and sampling.
The reference voice is extracted from our bound generated Mandarin sample, not a
claimed human-approved voice recording. Final generated speech still needs listening.

All three Stock20 arms have now finished: native-v2 has the independent successful
inference audit (its disconnected node1 caused the original controller failure),
original-v3 and bunny-v3 have successful controller terminals and cleaned services.
`hybrid-three-arm-audit-v2.json` compares the actual stored active graphs, permits
only that unreferenced loader removal, Bridge input and output filename differences,
and verifies identical runtime sources, Hybrid artifact and voice reference.
All73-frame512×768 outputs strictly decode; generated PCM differs from the voice
reference. This is generated-audio evidence, not proof of voice similarity or quality.

## Regression repair qualification

`full-cpu-v3` ended with5993 passed,27 failed,197 skipped; do not call it green.
The repair scope `regression-repairs-v1` subsequently passed all249 selected tests,
including every failed area and the previously crashed isolated outpaint worker.
Its1691 source hashes remained unchanged and CUDA stayed uninitialized. The old
worker exit3221225477 remains unexplained; a successful repeat is not a cause proof.
Repairs preserve the261 published workflow bytes and original207-workflow digest:
missing historical fixtures were restored byte-for-byte; old registration/count
assumptions, leaked test-only PROJECT globals, explicit UTF8 reads and a fake VAE
that incorrectly mixed per-tile batch means were corrected. No tolerance was relaxed.
The already-published speech workflow delta is pinned to its exact release commit
and hash instead of broadly allowing workflow changes. Final integrated regression
must still be run after all remaining runtime work.

## Still required

- Dense Sage and Sol with FastV2 dense-compat, T8 head4/FFN2 and original Bridge
  completed independently: `p3-dense-kj_sage-h4c2-original-v1` and
  `p3-dense-sol-h4c2-original-v1`. Each reports eight completed model forwards,
  1600 selected backend calls and two PyTorch calls; Sol retains the authenticated
  non-video prefix. Both73-frame H264/AAC clips strictly decode and owned services
  are cleaned. Neither media acceptance nor a speed comparison is inferred.
- `package-v2-cli-examples` actually extracted/imported341 nodes on CPU and ran
  the packaged conversion CLI help. It includes the five EXP workflows in
  `examples/workflows/34-semantic-bridge`; the261 published graphs remain unchanged.
  Archive SHA256 `ddbffb85b1027a5c7b26a1b3d5842267ec3c74bea9df8d7f6dc4cb5d3f8034fd`.
  This is a frozen Bridge snapshot, not qualification of later H16 runtime files.
- Both fixed loop interruption recipes are mechanically complete. Preserve their
  evidence and original failures; no need to regenerate for the final review.
- Sage/Sol, Hybrid and generated reference-audio clips remain human-pending.
- Final integrated full CPU regression; repaired-scope success is not a full pass.
- Final combined package and formal-core precision deployment that preserve
  unrelated user changes and original defaults; don't overwrite the observed
  formal/development EAV default overlap with a wholesale file copy.
- Central human review of picture, generated speech/music/singing, and seams.
- Continue H16 remaining deliverables and Meridian P0/P1/P2 per the active goal.

No commit, push, Registry publication, or copying unreviewed examples into a released
recommendation is authorized by this ledger. Roadmap remains local-only.
