# 渐进采样组合适配（开发候选，未完成成片验收）

2026-09-17覆盖下方历史：正式本地执行器已补齐原有SelfLift调用方的可选接口；
现有公开节点／示例不再因旧签名缺checkpoint、producers或eav_tau而失败。
见[本地接口修复](SELFLIFT_INTERFACE_REPAIR_20260917.md)。尚未发布此修复到Registry。
下方候选进展、GPU与审片说明保留其原时间和范围，不能作为此次修复后的新质量证据。
小型原生 H3 的 CPU 执行测试不等于真实权重、CUDA 内核、显存、速度或成片质量通过。
已有双 MODEL 长视频的接缝、音频与恢复逻辑没有被这条渐进路线替换。

公共接口最新进展：开发树新增独立Progressive Setup与Progressive Long Video两节点，
两路MODEL/原生完整SIGMAS/独立TST/内部EAV/Relay可显式接线，真实CPU服务API图已校验。
新增恢复关闭语义在OS锁内拒绝已有链、绝不删除。真实浏览器往返与完整模型两段8秒的机械核验已有记录；其余组合和人审仍待，未部署。
详见 [渐进长视频公共节点](PROGRESSIVE_LONG_VIDEO_EXP.md)与[公共工作流接线](PROGRESSIVE_PUBLIC_WORKFLOW_EXP.md)。
完整权重 Core/Sage 的单首帧3秒基准也已独立核验实际4+4、400次Sage调用与完整音视频，
未观察到PyTorch回退；这只是该固定配置的执行证据，不是画质或加速验收。
guide、TST、EAV、Sol及多功能组合仍按单独样片验证，不把基准通过推广到所有开关。

## 采样与接线

渐进节点使用一张完整的原生 Euler 时间表。总8步、`low_evaluations=4`表示低分辨率4步，
学习型3D latent放大，然后完成剩余4步；`low_evaluations=6`为6+2。
这不是原双 MODEL 长视频节点的独立二采时间表，也不是VDN8+4。

两路分别配置：

- LOW：原生H3底模 → 本路兼容H3 LoRA链 → 本路一个注意力后端 → `model`。
- HIGH：同一或兼容底模 → 本路兼容H3 LoRA链 → 本路一个注意力后端 → `model_hires`。
- 不接`model_hires`则沿用LOW配置；不能通过在LOW链后再叠加LoRA来获得独立HIGH配置。
- 两路架构、latent格式、视频/音频shift及音频尺度必须一致。权重可以不同，但兼容形状不保证画面风格或人物连续。
- 旧单段 `MiniMaxH3ProgressiveSamplerEXPT8` 的公开输入仍要求空原生AV潜空间。
  新 `MiniMaxH3ProgressiveLongVideoEXPT8` 在内部管理首帧、续段条件和阶段缓存，已有两段8秒机械证据；
  它不公开任意初始化AV、多参考或通用编辑输入，不能以内部测试接口代替公开支持范围。

Core注意力选择、指定已审计KJ Sage/省显存/FFN组合、指定`SolAttentionPatch`分别是可选路线。
多个attention替换器可以由用户自行选择，后置入口可能覆盖或绕过前置功能；
保留可委托callable并报告真实覆盖，不因未验证主动阻断。Sol在偏置、不等长Query等情形
会明确回退；这不是稀疏加速证据，第三方节点自身真实内核合同不在本次改动范围。
Core Sage报告中的delegate调用也不等于独立证明底层CUDA内核无回退。
对不接收Relay偏置的Core自动Sage版本，渐进组合器仅将有偏置查询交给已审计的
Sage Triton作用域内核，保留自然对数偏置到log2的换算；`scoped_masked_sage`单独报告实际调用。
找不到已审计的偏置内核时保留明确的SDPA回退原因，不丢弃偏置，也不假报Sage执行。

## Prompt Relay

单段采样接口的LOW应接当前Prompt Relay Conditioning节点的配对MODEL与positive输出，`cfg=1`。
其HIGH可以接未绑定Relay的独立MODEL，或相同绑定的Relay MODEL；不同剧本/时间表的两路绑定被拒绝。
新Long节点则直接接全局 `prompt_relay_plan`，内部按每段和两个画幅准备条件；不要额外给它叠加另一套已绑定Relay MODEL。

节点会核对原目标画幅，再给LOW和HIGH分别建立精确packed layout及配对hash。
只改变空间网格；局部事件时间、文本token范围及音频时间坐标保持不变。
`video_only_paper`只直接路由视频Query；`joint_av_exp`同时路由目标视频与音频Query。
全局提示词放持续的人物/衣着/环境，单次对白放对应局部事件，不能重复写入全局。

执行报告分别记录每阶段的完成前向数与真正经过Relay路由的主注意力次数。
错误配对、真实目标尺寸漂移仍报错；主网络路由被用户补丁绕过时记录覆盖不足、
标记unverified，不假报Relay生效，也不以覆盖不足禁止用户运行。

## EAV

推荐使用渐进节点内部的`eav_mode`，内部显式组合器让本路EAV与Relay共享网络入口。
已有外部包装不因未经验证而被禁止；但额外增益或不委托实现可能改变效果或绕过功能，
须检查实际执行报告。节点自身绑定损坏、实际错误坐标／mask与内核异常仍校验。

| 参数 | 含义 |
| --- | --- |
| `eav_mode=disabled` | 默认关闭；不安装EAV，不修改采样张量 |
| `report_only` | 计算并报告CFI和增益，但不应用增益；CPU对照要求与关闭时逐位一致 |
| `apply_exp` | 实际对目标视频attention输出应用FETA增益；后续联合层仍可能间接改变声音 |
| `eav_tau` | 增益公式中的调节量；0不代表关闭 |
| `eav_start_video_progress` / `eav_end_video_progress` | 使用`1-原生视频sigma`，不是当前阶段的步数百分比；HIGH不会从0重新开始 |
| `eav_max_workspace_mib` | CFI分块计算预算，不是整机显存上限 |
| `eav_g_hard_limit` | 增益超过上限中止；不静默截断或重试 |

当前新增范围是完整8或20步、CFG1、T2VA/单首帧I2VA。完整表必须从1到0，LOW至少1步，HIGH至少1步。
每阶段的实际sigma、进度、帧数、空间网格、音频范围、前向和主块测量次数都单独核验；
两阶段合计必须对应原完整时间表。即使某阶段没有落入EAV窗口，也仍核验该阶段的采样执行。
若全程没有激活EAV，报告明确写`inactive_window_no_enhancement`，不声称增强生效。

这是新的渐进EAV8实验，不是解除原双MODEL长视频的Stock20限制。
原长视频4+4流程目前仍不能通过直接打开原EAV开关来获得这一新增组合。

## 首帧缩放

`guide_resize=legacy_bilinear`保留原默认。`preserve_mean`可选地保持LOW首帧latent每通道空间均值；
不混合时间维度、不修改HIGH原始参考。该选项是否改善色彩还需固定素材的真实模型对照。

## 交付前仍需完成

公共接口、浏览器往返、两段8秒及Core/Sage基准的机械检查已有独立记录，不能再当成“尚未接入”。
尚待完成的是其余完整模型组合、所有新片的动态人审、按通过范围晋级的正式示例及其最终UI/API复核，
以及基于最终文件的文档和包边界审计。预打包通过也不能替代最终交付检查。
未验收候选不能混入“已通过”示例，旧通过片也不能冒充新数值路径的质量证据。

### TST内部组合进展

已接入原生渐进采样与续段的显式内部TST作用域，默认关闭。采用有符号Query修正的
`signed_query_mean_frame_v1`帧均值代理；不是作者仓库当前的输出增益算法，也不宣称精确空间算子。
顺序为TST完整Query修正 → 原Relay偏置/分块 → 原后端 → EAV输出增益。
实际小型H3、双阶段、KJ/Sol、续段独立LoRA链、取消和LOW缓存恢复已有CPU验证；
开发版已追加独立TST MODEL节点，普通原生Euler及LOW/HIGH分别配置均有CPU执行验证；
节点进入渐进采样后的小型CUDA矩阵也已通过。仍未部署、未完成完整权重GPU/TST成片测试。
详见[TST来源、接线与约束](TST_SOURCE_CONTRACT_EXP.md)。

## 内部接口与防回归实施记录（历史，非当前公开使用说明）

以下保留实施阶段的数值契约、失败修复与分批测试记录。其“尚未接入／TST尚未实施／只有CPU”等
进度描述仅属于记录当时，当前状态以上文和公共工作流说明为准；各历史测试数量不能相加冒充本版全量回归。
内部函数可接受的参数不自动成为公开节点功能，历史机械结果也不自动成为新片人审结论。

### 初始化AV与遮罩

### 整条音频与每段音频（内部任务接口）

`NativeProgressiveJob` 的 `shared_condition_options` 接受全片共用的条件选项。
其中 `drive_audio`／`final_audio` 被视为整条输出时间轴音频：每段按
`timeline_start_seconds - context_frames / 24` 开始，截取完整 render window，
源不足时仅补零，不循环，不改变采样率。切窗复用已有长视频实现，一次只准备当前段。
`audio_mode=reference_only` 的 drive_audio 是音色参考，保持原对象，不按时间轴耗尽。

已有 `segment_condition_options[index]` 仍表示调用者已经选好的局部条件／音频；
显式局部字段优先于同名共享字段，局部音频不再切窗。局部 `None` 可以取消同名共享输入。
因此不能把整条音频放到每段局部输入后期待自动裁切。
共享尾帧只交给最后一段；首帧复用与身份策略仍由原条件构建器处理。
参考视频／参考图片不是输出时间轴源，不会在这个辅助层被自动切片；原有任务支持检查仍有效。

`delivery_audio(index, generated_audio)` 在实际任务锁下，从内容绑定的输入重新选择输出：
显式 `final_audio` 优先；否则 native／lock_source 使用 drive_audio（若连接）；
reference_only／remix_source 使用生成音频。返回来源与实际内容身份，完成 latent 缓存命中后也可重建。
这些规则与原条件构建器一致，不把音色参考或重混的源音频误当最终音轨。
共享输入全内容纳入 job 身份，源音频／选项发生变化不能复用旧任务。

下述内部封装已接入自动解码、机械接受、拼接和最终媒体恢复，但仍需完整模型组合验证，
不能替代新成片的人审。
新增媒体选择、首段采样／恢复和旧循环合计51项CPU回归通过；首段执行使用真实小型H3/Euler，
编码器及学习型放大器为明确测试替身，不是完整权重的视频质量证据。

### 自动交付链（内部候选接口，CPU交付验证通过）

`ProgressiveChainDelivery(job).run()` 接收已绑定实际模型、编码器、时间表和源媒体的
`NativeProgressiveJob`。只在 ComfyUI 配置的输出目录下工作，复用同一条链的独占锁；
不修改全局输出目录，不替换旧长视频节点。当前固定24fps、8bit H.264/AAC交付。

每段流程为：首段／续段采样及其已有阶段缓存 → 原生AV解码 → 原输出音轨选择 →
裁掉上下文并限制到计划时长 → 原候选MP4／39帧容量上下文保存 → 内容绑定回执 →
原子接受。所有片段就绪后调用原逐帧流式拼接器。这里的“接受”只是机械执行状态，
结果始终标记 `human_qualified=false`，仍需完整动态审片。

- 在候选回执完成后、接受前中断：验证候选与源任务后继续接受，不重新采样或解码。
- 在最终拼接中断：已接受片段保留，继续拼接，不重跑片段。
- 最终回执存在：重新校验任务、交付设置、全部候选／接受媒体与最终文件哈希后复用成片。
- 半成品没有完整回执：保留旧文件，新的编码尝试使用新候选名；不覆盖或盲目采用残留物。
- 修改编码设置／前缀／音频接缝策略后用新链；坏回执、坏哈希、外来接受片段立即拒绝。
- 主清单损坏或只有旧备份时不静默回退，以免丢掉最后已提交片段；需先明确恢复主清单。
- 写入锁／回执／候选／接受文件前，检查对应目录和目标路径仍在链目录内；拒绝越界重定向。

此封装不增加视频叠化、颜色修正或latent桥，不修改HIGH遮罩、音频latent和3D放大器。
真实编码／拼接和中断恢复已通过CPU测试；尚不能把该封装算作完整权重GPU成片通过。
目录边界检查加入后，交付、实际任务恢复与旧双采接缝／工作流合计108项回归通过，
包括实际192帧两段MP4、接受／拼接中断、最终复用和真实目录重定向拒绝。
随后补严了最终复用前的接受文件路径非空／上下文存在性检查，11项聚焦检查通过（与前述有重合）。
交付测试的采样和VAE解码为明确替身；实际tinyH3采样恢复测试另有覆盖，两者不冒充
同一次从完整权重生成到成片的端到端验证。所有新片质量仍需真实模型生成与完整动态人审。

### 实际条件与交付报告绑定（内部候选）

首段及续段统一输出 `prepared_delivery`：实际条件构建器返回的 conditioned_prompt、
媒体标签表、条件报告，以及所选 mux_audio 的全内容身份。续段的这些值纳入准备输入身份，
生成过程中被修改会拒绝继续。交付不再用原始 segment_prompt 冒充实际带参考标签的编码提示词。
候选恢复时核对保存提示词；交付时核对显式音频身份，生成音频路线则必须没有外接mux覆盖。
这不重新编码提示词、不改音频、不改变生成数学，只让持久报告绑定真实执行输入／输出选择。

两种同一次整链CPU验证已通过：T2VA＋联合音画Relay＋EAV＋显式最终音轨；
I2VA＋video_only Relay＋EAV＋锁源音频。原生小型H3/Euler、任务／阶段缓存、AV解码封装、
候选／上下文、MP4拼接连续执行，编码器网络与学习型放大器仍为明确替身。
这两条均完成192帧媒体与音频解码；T2VA另验证后段HIGH中断后仅执行剩余HIGH阶段，
两条最终媒体恢复均逐字节一致且不再次采样／解码。与三项准备报告篡改拒绝检查同批5项通过。
不把随机小模型、替身VAE输出当作画质或完整模型验收。

另有3项原生续段组合测试通过：PyTorch、已安装KJ省显存定义、已安装Sol选择器。
每路LOW／HIGH各串联两个真正的Core LoRAAdapter，使用不同权重和强度；
在实际网络前向入口逐次核对应用后的权重与独立矩阵乘法预期值，而非只数补丁条目。
同时验证联合Relay、EAV、原生续段遮罩和实际4+4前向。CPU回退如实记录，不是Sage／Sol的CUDA提速证据。

内部`input_mode=initialized_av_exp`允许有限值原生AV源；默认`empty`继续拒绝非零源和遮罩。
输入时间表可从`0 < sigma <= 1`开始，必须严格递减到0并至少保留各阶段1次前向。
完整表从1开始时，原生flow会抹去未遮罩区域的初始化内容；这不是有保留强度的img2img。
需要初始化内容影响自由区域时必须显式提供部分时间表，不能只接非零latent便宣称在细化原片。

遮罩`1=生成、0=保留`，普通张量只控制视频，音频仍为全生成；联合遮罩可分别控制音视频。
视频时间只能为1（逐帧广播）或精确latent时间长度；音频时间同样不做隐式拉伸。
空间缩放调用Core原始运算顺序，避免“数学等价”的二维插值引入不同fp32舍入。
重复的完整通道遮罩可以输入；不同通道的不同遮罩尚未支持，不能静默挑第一个通道。

两个状态必须独立：

- HIGH求解器仍从LOW预测、原有学习型3D放大和原生音视频状态重建获得带噪起点。
- HIGH的已知区域始终绑定原始目标尺寸的干净AV源，不使用带噪起点或LOW半成品充当干净参考。

实现仅在本次原生Euler的临时`KSamplerX0Inpaint`对象上绑定干净源与条件噪声，
保留Core输入注入、2×2视频token遮罩、音频尺度/时间转换及输出约束；结束或取消时恢复临时对象。
这不是采样后修补画面或简单post-CFG锁定，也没有增加扩散前向数。

CPU对照覆盖全已知/全生成/前缀/分数遮罩、部分时间表、原生输入张量与条件、取消重用、
Relay以及两路各两层真实Core LoRAAdapter计算。Sage内核仍用明确标注的CPU测试替身，
Sol在CPU走明确报告的fallback；这些不构成CUDA加速或训练权重画质验收。

初始化AV与EAV已增加内部显式组合器（仍未开放为正式节点用法）：

- LOW/HIGH分别绑定各自精确画幅、音频形状、原生token遮罩及内容hash；每次前向都核对实际接收到的遮罩。
- 支持完整或显式部分时间表；进度仍为`1-sigma`，HIGH不重置，步数与原时间表逐段核验。
- FETA沿用整个目标视频网格的CFI/增益定义，包含已知和可生成帧，不冒称“仅统计自由区域”。
  原生H3仍负责已知区的输入注入和最终约束；后续联合层可能间接影响自由音频。
- `report_only`与关闭时要求逐位相同；全已知区域即使内部计算了增益，最终仍受源内容约束，不能据此声称画质改善。
- 新的内部profile不加入原EAV节点的选项，不放宽旧Stock20、参考块、STG或原长视频组合的保护。

公共初始化接口尚未接入前段来源、视频VAE续段构建和恢复身份，不能据此宣称“长视频已经支持”。
既有通过样片的LOW参考来源、HIGH遮罩、音频和缓存路径没有迁移到此接口。

### 续段来源、原生采样与 Relay／EAV（内部开发进展）

新增内部 `progressive_continuation.py`，复用原有接受成片路径，不改旧 runner：

- 从当前主 manifest 核对被选择的直接前段、revision、执行合同、画幅与24fps；
  校验候选与 accepted 两份视频、上下文的实际文件 SHA，拒绝另一任务、final前段、越界路径和坏数据。
- LOW 使用 accepted MP4 实际末39帧 RGB24，依次执行既有 resize 和视频VAE编码；
  零值张量仅作编码器输出的形状/精度模板，从不作为LOW采样来源。
- HIGH 使用前段完成后的原生AV上下文。两路使用同一个完成音频尾部，保持 overhang；
  39帧仍只是保存容量，22/39上下文分别选择7/12个视频latent时间单元。
- 分别调用既有长视频条件构建器，随后仅给HIGH加既有原生视频前缀mask；
  native/lock_source/remix_source的音频条件与遮罩不被重新定义。
- 不可变来源描述可纳入后续阶段缓存合同；每次准备前后重新检查实际文件与manifest。
  调用方仍须持有链的排他锁，并提供由实际MODEL/LoRA/VAE/提示词/代码计算的完整执行合同SHA，
  不能把任意64位字符串当作模型身份认证。该完整公共入口尚未接好。

CPU测试包含实际小MP4解码、真实Core PackedLayout与旧时间修复函数、取消及坏来源拒绝；
编码器/CLIP使用明确的测试替身，不是训练VAE或成片质量测试。
新增内部 `sample_progressive_continuation` 已将准备结果接到原生H3/Euler：

- LOW使用独立条件与初始AV，不把HIGH的前缀遮罩缩小后强加给LOW；
  HIGH继续使用原生干净上下文和前缀遮罩，音频继续原生LOW边界状态。
- 输入全局 `prompt_relay_plan` 时，从已接受直接前段的实际 `timeline_end_frame` 确定续段起点，
  复用既有投影函数减去上下文重叠。LOW/HIGH分别建立含motion参考的PackedLayout，
  事件midpoint平移，原窗口宽度、sigma、文本范围和音频坐标不重新估计。
- Relay与EAV使用一个显式组合入口，逐阶段核对实际motion payload、原生遮罩及每次主网络调用；
  `joint_av_exp`不能搭配锁定源音频，仍需选择`video_only_paper`。
- 同一准备对象绑定实际条件张量、AV、遮罩、事件及来源身份。修改绑定、错配投影或前段时拒绝执行。
  LOW/HIGH独立LoRA链、KJ分头/FFN与Sol路由有CPU组合测试；CPU Sage替身和Sol回退不算CUDA提速。

EAV数值检查特别记录：Core Euler最终仍计算 `x + ((x-denoised)/sigma)*(-sigma)`，
不是直接返回denoised。开启增益会改变轨迹，已知区最终数值可能因此有fp32舍入差。
测试通过真实最后一步回调，要求**完整视频输出与原生Euler重建逐位一致**，
并要求**已知区域的denoised与原始干净上下文逐位一致**；不改采样结果、不增加后处理，
也不把“两个不同轨迹的最终张量必须逐位相同”当作Core原生保证。
`report_only`与关闭EAV的完整AV逐位一致检查仍保留。

这些是单个续段的内部执行接口，公共节点仍不接受任意motion条件。
完整阶段缓存（尤其带噪audio_next）、执行身份、自动多段循环和断点续跑尚未完成；
也没有新增真实训练权重、学习型lifter/VAE、0.4MP两段8秒或人审验收。
不能把上述CPU结果称为已完成长视频正式交付。

### LOW边界缓存：新增内部恢复接口

`ProgressiveCheckpointSession`现可在内部调用中保存和恢复已完成的LOW阶段。
它不是旧长视频的`low_x0`缓存，也不代表整个多段链已经支持恢复。

- 同时保存fp32视频干净预测`clean_video`与**仍带噪的原生音频状态**`audio_next`。
  HIGH继续后续音频采样，不能把这个音频状态当作已完成音轨锁死。
- 本地目录使用操作系统排他锁；完成标志是原子写入的JSON回执，绑定safetensors内容SHA。
  残留的孤立tensor文件不算已完成，也不会被自动删除。
- 缓存绑定两路实际模型权重、顺序LoRA内容、采样坐标、后端、条件与AV内容、种子、计划、
  放大器身份和实现源码。条件准备仍在查缓存之前执行；完整CLIP/VAE生产者合同与整链入口尚待完成。
- 命中LOW时，本次实际前向计数是LOW=0、HIGH=4；历史LOW的EAV/Relay证据单独注明，
  不伪造当前回调或把旧执行计入新后端调用数。支持的内部组合包括续段投影Relay/EAV；
  首段外部已绑定Relay的缓存入口现已增加，范围见下方专节。
- 中断恢复的CPU测试在新的Python进程重新建立模型和两路各两层真实LoRAAdapter，
  对plain、KJ省显存/FFN、Sol三路核对完整AV逐位一致。Sage使用标明的CPU替身，Sol是明确CPU回退；
  放大器/CLIP/VAE同样为测试替身，不是CUDA、训练权重或成片验收。

本次发现并修复两个恢复问题：

1. Core在采样后保留已加载模型；临时组合克隆与原输入共享模块，因此KJ/motion对象补丁可能残留。
   每阶段结束或取消后，通过Core的`unpatch_model(unpatch_weights=False)`撤下本阶段对象补丁，
   保留权重驻留、LoRA备份和原始补丁描述。下次Core管理加载会重新应用该阶段补丁。
   恢复前检查实际对象归属；发现外来替换不覆盖，不以放宽身份检查掩盖问题。
2. KJ的FFN配置在内存中是tuple，JSON落盘变为list。模型身份在创建和复核时使用相同JSON规范化，
   不再把相同配置误判为变更。数值/内容变化仍必须使旧缓存失效。

回归要求包含正常结束、LOW/HIGH取消、外来对象拒绝覆盖、新进程恢复、完整音视频逐位对照，
以及有/无对象清理的采样数值等价。原双模型长视频runner、缓存格式、接缝路径均未替换。
HIGH结果缓存、最终段交付、完整自动循环、训练模型8秒两段和人审仍属于未完成交付项。

2026-09-13相关27文件CPU回归：661通过、13跳过、0失败。跳过项为12个显式CUDA用例和
1个仅由父测试启动的工作进程入口；三个新进程CPU恢复实际执行成功。旧接缝/前缀/缓存/节点门禁
也在本轮范围内。这是当前源码的CPU证据，不继承旧小模型CUDA结果作为新清理路径的GPU资格。

### 原生CLIP／VAE生产者绑定（内部接口）

新增`NativeProgressiveProducers(clip=..., video_vae=..., audio_vae=...)`，绑定的是实际原生组件对象，
不是文件名、UUID或调用方提供的SHA字符串。将该绑定通过内部`producers`参数传给续段接口时，
会在条件准备前后核对实际传入的三个对象和内容；采样开始、结束及缓存读写时继续复核。
缓存键包含生产者身份，因此即使已准备的latent完全相同，更换VAE配置也不能命中原缓存。

检查内容包括实际权重、完整分词器词表与选项、原生组件和网络执行方法/源码、配置、精度/设备策略，
以及不进入state_dict的buffer（例如视频VAE的pixel_mean/pixel_std）。实例方法替换、网络forward替换、
未知执行对象和未经适配的组件权重补丁会被拒绝；不靠对象repr推测身份。

原生CLIP每次编码重设的layer/device临时状态、已返回条件中的last_token_tags，以及Core驻留计数和
comfy_patched_weights等加载状态，不冒充持久配置。真正控制它们的CLIP层选择、options_default、
patcher设备/force-cast策略仍绑定；分词表、实际条件token与权重内容变化仍会失效。

测试使用原生包装器、真实分词器及微型非生成网络；另以meta构建完整CLIP和两种VAE检查配置遍历，
并明确要求meta权重不能通过实际内容身份检查。Core加载/卸载对照不执行编码。
这些不是训练编码器、量化权重、DynamicVRAM或真实成片验收。

当前仍为内部显式绑定：无`producers`的低级接口仅绑定已准备输入，不能宣称完整生产者认证。
完整公共多段入口还需强制该绑定并将其纳入链执行合同；HIGH/最终段缓存和自动循环
仍待接入。这个新增接口不替换原长视频的生产者检查、VAE顺序、上下文或拼接。

### 首段已绑定Relay的LOW缓存（内部接口）

首段的独立Prompt Relay MODEL/positive现在可以与内部`ProgressiveCheckpointSession`组合。
默认未提供checkpoint时执行路径不变，公共节点尚未新增缓存控件。

- 仍先校验实际Relay包装器和配对conditioning，再建立LOW/HIGH空间布局，不接受任意合同SHA。
- 缓存额外绑定完整事件、时间、视频/联合AV路由、query分块大小、原始Core后端或已审计后端配置，
  以及原始两路MODEL内容和配对文本。去掉临时Relay包装器不等于可以丢掉这些身份。
- LOW保存后，HIGH中断可从LOW边界恢复；本次仅执行HIGH剩余4步，继续原生带噪音频，
  历史LOW的Relay/EAV统计与当前执行分开。没有增加采样步或修改放大、sigma、接缝。
- 身份检查覆盖输入绑定和执行结束；准备后更改事件/文本/权重会拒绝交付，合法新配置产生新缓存键。
- Core撤下Relay包装器会留下空wrapper类型字典。适配仅删除这个空容器，不移除实际存在的其他包装器。

独立Relay Conditioning节点沿用原先的“先绑定Relay，再接LoRA”限制；不能将内部的无权重检查克隆
误解为可以跳过公共节点的输入保护。两路LoRA仍独立，HIGH可以是未绑定Relay的兼容MODEL。
Sage CPU替身、Sol明确回退和微型H3的恢复测试不代表训练权重/CUDA/媒体验收。
完整任务合同、多段自动循环、HIGH/最终段缓存和真人成片验收仍待完成。

2026-09-13本次29文件相关CPU回归708通过、14跳过、0失败；包含原生生产者身份检查和新增首段Relay缓存。
14跳过为12个显式CUDA用例与2个子进程专用入口。父测试实际启动并验证了六次独立进程恢复：
原续段三后端与新增首段三后端，均核对完整AV逐位一致；新增首段组合包括EAV、两路各两层LoRA。
本次不包含新的GPU或训练模型成片，不将这些数量相加到旧回归冒充更多独立验收。

## 实际任务合同与锁定续段入口（内部开发接口）

`NativeProgressiveJob`直接接收两路原生MODEL、sampler/sigmas和实际CLIP、视频/音频VAE。
它必须构建原生生产者身份，不接收调用方提供的job SHA作为身份依据。
总时长、render window、22/39上下文、提示词和seed策略交由已有长视频计划器生成逐段计划。

合同覆盖实际模型/顺序LoRA、生产者内容、完整sigma、尺寸、全部逐段提示词/seed/时间范围、
Relay完整计划及query分块、每段条件选项/媒体张量、EAV等采样选项、放大器和实现源码。
合同只返回独立JSON副本；使用前后复核实际输入，改变配置后不能沿用旧链。

`exclusive(root)`取得与旧长视频相同的操作系统链锁，校验已有验收清单与不可变任务合同。
仅在确实取得锁后才释放，不因争锁失败尝试解锁别人的任务。
`sample_continuation(index)`自行从当前验收清单选择直接前段，核对其时间范围，再用计算出的
任务SHA和绑定的生产者进入既有LOW成片参考/HIGH原生上下文路线；内部LOW缓存仍继续带噪音频。

续段HIGH完成后，任务入口会另外保存完整原生AV阶段，使用已有不可变AV序列化器，
绑定实际任务合同、直接前段及该段计划。它与LOW的`clean_video/audio_next`边界分开。
如果HIGH也已经完成，恢复时不重新准备条件、放大或采样；当前前向数明确为LOW0/HIGH0，
旧的Relay/EAV/采样报告放在`historical_sampling`内，不冒充当前执行。
读取时核对tensor SHA、完整形状/fp32/有限值、报告摘要和HIGH完成计数，坏缓存直接拒绝。
这只是完成的AV阶段，尚不代表视频已解码、被接受或成功拼接交付。

这是实际可调用的内部阶段入口，不是已经完成的自动长片节点。
每段媒体选项目前必须已经选取/裁好，不把同一整条音频自动当作每段正确来源。
逐段decode/accept/assembly、最终媒体缓存以及公共UI与整链恢复仍需接入。

### 第0段任务入口（内部接口，CPU阶段验证）

`sample_first()`在同一任务锁和内容合同下准备并执行第0段，保留LOW边界缓存，并另存完整HIGH AV。
完成缓存命中不再编码条件或执行采样；已有被接受段时拒绝重新生成第0段，应转入下一计划段。
当前准备范围是T2VA／单首帧I2VA以及不添加音频参考块的源音频遮罩；多参考条件仍明确拒绝。

首段复用原long-video条件构建器，确认没有上下文后只移除该构建器产生的schema标记。
未知metadata不清除，仍受原生渐进条件校验；不放行任意motion／额外模型包装。
Relay复用原全局计划的第0段投影与真实token绑定，保留单事件不安装补丁的语义。
`joint_av_exp`仍拒绝锁定源音频组合；不删除偏置来获得后端加速。
`guide_resize`已纳入实际任务身份，只作用于首段参考缩放，默认保持`legacy_bilinear`。
首段准备报告保留条件、媒体映射和交付音频身份；保存AV不等于已经完成解码／混流／接缝验收。

新增测试首轮抓到适配器误取投影函数返回值的问题，已修正为使用第二项编译提示词，
没有放宽原Relay的提示词一致性保护。修正后首段Relay＋EAV的完整4+4、HIGH中断、
LOW缓存恢复及完整HIGH缓存恢复已在小型原生H3 CPU执行中逐位对照通过。
独立原生空latent轨迹对照及六组既有接缝／前缀／缓存／节点／工作流文件回归另有91项全部通过，
0失败、0跳过。原生CLIP/VAE身份使用组件壳，实际编码与学习型放大器仍为显式测试替身。
两批有重叠，不相加为一次全仓回归；不引用先前253／708项代替本次代码验证。
训练权重、真实VAE／3D放大器及完整自动链的GPU和动态人审仍待完成。

可选TST的论文／作者代码／SelfLift三种实现差异见[TST来源核对](TST_SOURCE_CONTRACT_EXP.md)。
TST尚未实施，不把来源分析等同于已支持。
旧正式节点、已验收工作流和接缝数值路径未替换。

本次14文件相关CPU回归253通过、2个子进程专用入口跳过、0失败；包括任务内容变化拒绝、
真实链锁、实际前段选择，以及不中断基线/HIGH中断/LOW恢复/HIGH完成缓存的完整AV逐位对照。
这是微型原生H3执行；编码器采用原生身份测试壳和明确的条件生成替身，放大器也为测试替身，
不是完整训练编码器、学习型放大器或GPU成片验证。该253项不与旧708项相加计数。
随后对同一端到端用例补充了完成报告损坏的拒绝检查并单独复测通过：拒绝时不重新准备条件，
不静默重跑。此补测不计为额外独立样片或GPU证据。

## 小模型真实CUDA证据的范围

隔离进程已在RTX4060Ti上执行随机权重两层H3（head_dim128、BF16）的双MODEL4+4测试：
两个阶段均在CUDA运行，每路两层真实LoRAAdapter参与权重计算；已知视频/音频区域受原生遮罩保护。
Core Sage、KJ Sage/分头/FFN和Sol分别检查实际调用，Relay偏置不能被删除以换取加速。
无Relay时Sol实际执行；Relay分块或有偏置查询明确回退，不声称Sol支持该数学形式的稀疏内核。

这组历史CUDA测试早于上述motion续段适配，不覆盖新续段Relay/EAV。
它使用明确标注的插值放大器替身，不含真实H3底模、学习型放大器权重、VAE或成片。
它验证CUDA算子和组合执行，不能替代完整模型的显存、速度、接缝、音画及人审验收。
