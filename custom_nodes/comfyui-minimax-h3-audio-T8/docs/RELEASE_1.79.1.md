# v1.79.1 — Accepted-picture continuation seam fix

## 发布内容

- 显式 `low_context_source=accepted_picture_low_context_v1`：上一段实际成片RGB尾部缩小、原生VAE编码，仅替换下一段一采的视频guide。旧JSON缺省策略不变。
- 保存一份用户完整审片通过的0.4MP／8秒／KJ／4+4示例，图内附中文参数、提示词、断点恢复和注意事项。
- 源视频哈希与策略绑定阶段缓存；命中缓存不额外编码；二采和音频不变，不增加扩散步。
- 配套保留已测的精度身份读取修复：优先读取PyTorch backend-specific精度，避免查询时混用新旧TF32 API；不修改用户精度设置。
- 效果循环恢复时使用与接受记录完全相同的sampling-summary后缀，不降低校验或吞掉冲突。
- 文档记录原因、失败路线、精确样片身份和后续更新门禁。没有发布失败latent bridge、失败对照工作流或其他无关开发功能。

327节点，236份工作流。详细说明：[接缝修复与防回归](DUAL_MODEL_SEAM_FIX_20260913.md)。

## 验证范围

用户验收的是固定8秒候选（视频SHA在修复文档），正式移植保留RGB24→末39帧→已有resize→原生VAE的数值顺序。
正式集成最终聚焦回归 **145项通过**（`seam-final-tests-v2.xml`），覆盖新策略、22/39坐标、二采前缀、音频、恢复、精度身份、已有3D放大和发布元数据。
新图14个执行节点、14条连线、75个显式参数通过独立序列化检查，并按当前节点schema校验参数。它不是一次新的浏览器导入实测。
官方打包器实际构建、解包、导入通过：327节点、236图，CUDA未初始化，损坏媒体子进程按预期拒绝且无残留。Ruff与版本一致性检查通过。
打包器在生成环境Python中未安装，首次打包未开始；改用本机已有Comfy CLI环境成功构建，没有安装新依赖。最终发行包仍须在提交前核对解包结果。
本次没有重新生成视频或声称12秒最终策略GPU验证。旧4513完整回归不是新版本的全仓重新验证。
首段不改、仅后段LOW替换、HIGH/音频不动、缓存恢复及源身份拒绝由回归保护。

## 更新方式

更新节点包并重启ComfyUI，导入新日期的通过示例。首次使用新chain_id；中断后保持参数一致并设resume_existing=true。
未改旧图的缺省行为；旧任务不手工搬阶段缓存。KJ/Sage及模型仍由用户安装，不自动下载。
更换提示词、人物、尺寸、步数或后端后需要自行完整审片；本次接受不保证任意长片。
