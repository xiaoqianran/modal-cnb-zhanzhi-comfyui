/**
 * VNCCS Migration Extension
 * Legacy migration is now handled explicitly by the VNCCS Migration Assistant node.
 */
(function () {
    const TAG = "[VNCCS Migration]";
    console.log(TAG, "Automatic migration is disabled. Add the VNCCS Migration Assistant node to migrate legacy sheets.");

})();
