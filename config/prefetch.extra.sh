#!/usr/bin/env bash
# Optional extra prefetch lines. Same syntax as zhanzhi ``初始化下载``.
# Do not paste the upstream list here — that file is cloned from CNB.
#
# aria2c -x 8 -s 8 -c -d "/models/checkpoints" -o "example.safetensors" "https://example.invalid/model.safetensors"
